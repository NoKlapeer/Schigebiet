@model List<Schigebiet.Models.File>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <table>
        <tr>
            <th>File Name</th>
            <th></th>
        </tr>
        @foreach (File f in Model)
        {
            <tr>
                <td>@f.FileName</td>
                <td>@Html.ActionLink("Download", "DownloadFile", new { fileName = f.FileName })</td>
            </tr>
        }
    </table>
</body>
</html>
